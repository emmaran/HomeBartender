package org.launchcode.HomeBartender.models;

import java.util.ArrayList;

public class FriendsData {

    /**
     * Returns the results of searching the Friends data by search term.
     *
    /**
     * Search all Job fields for the given term.
     *
     * @param value The search term to look for.
     * @param allFriends The list of jobs to search.
     * @return      List of all jobs with at least one field containing the value.
     */
//    public static ArrayList<Friends> findByValue(String value, Iterable<Friends> allFriends) {
//        String lower_val = value.toLowerCase();
//
//        ArrayList<Friends> results = new ArrayList<>();
//
//        for (Friends friend : allFriends) {
//
//            if (friend.getName().toLowerCase().contains(lower_val)) {
//                results.add(friend);
//            } else if (friend.toString().toLowerCase().contains(lower_val)) {
//                results.add(friend);
//            }
//
//        }
//
//        return results;
//    }
}
